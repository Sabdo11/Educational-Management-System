<%= form_with(model: registration, local: true) do |form| %>
  <% if registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
        <% registration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :Student %>
    <%= collection_select(:registration, :student_id , @students, :id, :name) %>
  </div>


  <div class="field">
    <%= form.label :Section %>
    <%= collection_select(:registration, :section_id , @sections, :id, :year) %>
  </div>

   <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
